# Hero slideshow images

Order: 1=sanitation, 2=water, 3=electrical, 4=roads, 5=traffic, 6=health, 7=parks, 8=housing.

To refresh images: from `frontend` run `npm run download-hero-images`.
Uses Sharp for compression when available (see `scripts/download-civic-images.js`).
